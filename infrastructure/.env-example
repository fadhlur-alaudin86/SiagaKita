# Database Credentials
DB_USER=
DB_PASSWORD=
DB_NAME=siagakita
DB_HOST=postgres
DB_PORT=5432

# Redis Credentials
REDIS_PASSWORD=
REDIS_HOST=redis
REDIS_PORT=6379

# Server Ports
HTTP_PORT=8080
WS_PORT=8081

# JWT
JWT_SECRET=
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h

# Whatsapp Gateway
FONNTE_TOKEN=

# Email Gateway — Gmail REST API (menggunakan HTTPS port 443, bebas blokir SMTP)
# Memerlukan kredensial OAuth2 dari Google Cloud Console
EMAIL_FROM=
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# pgAdmin Login
PGADMIN_EMAIL=
PGADMIN_PASSWORD=

# Superadmin (satu akun, dibuat otomatis saat server start)
# Isi manual — jangan commit ke git!
SUPERADMIN_EMAIL=
SUPERADMIN_PASS=

# SMS Fallback Gateway (untuk endpoint /incidents/sms-fallback)
SMS_GATEWAY_SECRET=

# Docker Hub (untuk docker-compose.prod.yml di VPS)
DOCKERHUB_USERNAME=